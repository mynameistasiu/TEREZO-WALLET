{
  "name": "terezowallet-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean:next": "node -e \"try{require('fs').rmSync('.next',{recursive:true,force:true});console.log('.next cleaned')}catch(e){console.error(e);process.exit(1)}\"",
    "predev": "npm run clean:next",
    "dev": "next dev",
    "prebuild": "npm run clean:next",
    "build": "next build",
    "start": "next start",
    "build:mobile": "next build",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "next": "14.0.0",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.9.3"
  }
}
